{
  "slug": "chip-toggle",
  "name": "Chip Toggle",
  "group": "buttons-actions",
  "order": 40,
  "summary": "A filter chip that is a toggle button, not a checkbox — aria-pressed carries the state, and a tick carries it where color cannot. Four live failures: the color-only chip, the tick that renames its own control, the chip that renames itself on purpose, and a row that swallows the tab order.",
  "tags": [
    "button",
    "aria-pressed",
    "toggle",
    "filter",
    "toolbar",
    "roving-tabindex",
    "accessible-name",
    "forced-colors"
  ],
  "apg": "https://www.w3.org/WAI/ARIA/apg/patterns/button/",
  "wcag": ["1.4.1", "1.4.11", "2.1.1", "2.4.3", "2.5.3", "2.5.8", "4.1.2"],
  "status": "stable",
  "files": ["html", "css", "js"],
  "demoNote": "Examples 2 to 5 are live. Example 2's fill-only chip has no state left under Windows High Contrast, and the chip beside it puts a tick into its own accessible name; example 3's chip renames itself, so the word and the state disagree; example 5 puts five tab stops next to the toolbar's one.",
  "contract": {
    "useWhen": "A filter you switch on and off: a button that remembers, not a checkbox.",
    "root": [".ac-chip"],
    "aria": {
      "each chip": [
        "type=button",
        "aria-pressed"
      ],
      "a chip that is on or off rather than pressed": [
        "role=switch",
        "aria-checked"
      ],
      "the row": [
        "role=group",
        "aria-labelledby"
      ]
    },
    "keyboard": [
      [
        "Enter / Space",
        "toggles"
      ],
      [
        "ArrowLeft / ArrowRight",
        "move within the row"
      ],
      [
        "Home / End",
        "first / last chip"
      ],
      [
        "Tab",
        "one stop for the whole set"
      ]
    ],
    "states": [
      "pressed",
      "disabled"
    ],
    "failureModes": [
      "a checkbox instead — aria-pressed is what says a button remembers",
      "aria-pressed added only once pressed, so the control appears mid-page to a screen reader",
      "a fill and nothing else, so the pressed state is carried by color alone (SC 1.4.1)",
      "a tick typed in CSS content, which is folded into the accessible name",
      "the label text swapped on toggle, so the name and aria-pressed announce a contradiction"
    ],
    "api": [
      "AC.createChipToggle(root) -> { toggle, refresh, element, destroy }"
    ],
    "seeAlso": [
      "button",
      "switch",
      "checkbox"
    ]
  }
}
