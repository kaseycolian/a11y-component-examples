{
  "slug": "jump-nav",
  "name": "Jump Nav",
  "group": "navigation",
  "order": 20,
  "summary": "A list of links into the page you are already on is the easiest navigation there is to build, and the one most likely to leave a keyboard reader exactly where they were. Whether following a link moves the keyboard at all comes down to one attribute on the thing it points at. Knowing which section you are in is the harder half.",
  "tags": [
    "jump-nav",
    "in-page",
    "anchor",
    "aria-current",
    "scroll-margin",
    "intersection-observer",
    "landmark",
    "focus",
    "forced-colors"
  ],
  "apg": "https://www.w3.org/WAI/ARIA/apg/patterns/landmarks/HTML/nav.html",
  "wcag": ["1.3.1", "1.4.1", "2.4.1", "2.4.3", "2.4.7", "2.4.11", "2.5.8", "4.1.2"],
  "status": "stable",
  "files": ["html", "css", "js"],
  "demoNote": "Examples 2 to 5 are live. Example 2 presses the same link into three documents one attribute apart and prints what took focus; example 3 measures the overlap between the heading and the bar stuck above it; example 4 counts scroll events against section changes on one document; example 5 prints the landmark menu two navs produce with and without names.",
  "contract": {
    "useWhen": "In-page links to sections, where focus has to actually land on the target.",
    "root": [".ac-jump-nav"],
    "aria": {
      "the nav": [
        "aria-label"
      ],
      "the current link": [
        "aria-current"
      ],
      "each target": [
        "tabindex=-1"
      ]
    },
    "keyboard": [
      [
        "Tab",
        "after the jump the next stop is inside the target — if it took focus"
      ]
    ],
    "states": [
      "current"
    ],
    "failureModes": [
      "no tabindex=-1 on the target, so the page scrolls and the next Tab starts from the top",
      "tabindex=0 on the target, which buys a Tab stop on every heading in the document",
      "scroll-margin-top of 0, which parks the heading under the sticky header (SC 2.4.11)",
      "two nav landmarks with no aria-label, indistinguishable in a landmark menu",
      "the active section written to a live region per scroll event — throttle it"
    ],
    "api": [
      "AC.createJumpNav(root, options) -> { links, targets, current, destroy }"
    ],
    "seeAlso": [
      "skip-link",
      "tabs",
      "prose-surface"
    ]
  }
}
