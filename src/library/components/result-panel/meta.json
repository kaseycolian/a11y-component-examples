{
  "slug": "result-panel",
  "name": "Result Panel",
  "group": "feedback-status",
  "order": 40,
  "summary": "A result panel is what an action produced — a built URL, a key, a receipt — with a button to copy it. It is made almost entirely of other components, and each of them can announce on its own, so the work is deciding that only the panel speaks and everything inside it stays quiet.",
  "tags": [
    "result",
    "copy",
    "clipboard",
    "live-region",
    "composition",
    "output",
    "reflow",
    "forced-colors"
  ],
  "apg": "https://www.w3.org/WAI/ARIA/apg/practices/live-regions/",
  "wcag": ["1.3.1", "1.4.10", "2.5.3", "2.5.8", "4.1.2", "4.1.3"],
  "status": "stable",
  "files": ["html", "css", "js"],
  "demoNote": "Examples 2 to 5 are live. Example 2 measures how narrow the same URL is willing to be under three wrapping rules; example 3 builds one link into two panels and counts what a screen reader was handed by each; example 4 has three copy buttons reporting the same copy three ways; example 5 presses Copy on a panel that has nothing in it.",
  "contract": {
    "useWhen": "A computed value, with exactly one thing inside it that talks.",
    "root": [".ac-result"],
    "aria": {
      "the panel's region": [
        "role=status"
      ],
      "the copy button": [
        "aria-label"
      ],
      "a reason it will not act": [
        "aria-describedby"
      ]
    },
    "states": [
      "empty — aria-disabled=true on the copy button, not the disabled attribute, so it keeps its tab stop and can explain itself"
    ],
    "keyboard": [
      [
        "Tab",
        "the copy button is a stop; the value is a code element, not a control"
      ]
    ],
    "failureModes": [
      "an output element for the value — it has an implicit role=status and reads the whole value on every change",
      "four live regions in one panel, each defensible on its own",
      "the copy button renamed to a past tense, so the control under your finger changes",
      "a tick as the only confirmation, correctly aria-hidden and therefore silent",
      "overflow-wrap:break-word for a long token, which wraps the text and leaves min-content alone (SC 1.4.10)",
      "a refusal announced in the region — nothing changed, so put the reason on the button"
    ],
    "api": [
      "AC.createResultPanel(root) -> { set, copy, refresh, destroy }",
      "AC.setResult(panel, state)",
      "AC.copyResult(btn, options)"
    ],
    "seeAlso": [
      "notice",
      "status-text",
      "badge"
    ]
  }
}
