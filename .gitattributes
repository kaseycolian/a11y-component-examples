# Normalise line endings so the files people copy out of this repo are
# consistent regardless of the platform they were authored on.
* text=auto eol=lf

# Binary assets: never touch.
*.png binary
*.jpg binary
*.woff binary
*.woff2 binary
*.ico binary
